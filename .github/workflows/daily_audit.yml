# 核心片段：确保数据更新
- name: Run Ziwu Audit
  run: python main.py

- name: Commit and Push Data
  run: |
    git config --global user.name "Bot"
    git config --global user.email "bot@github.com"
    git add ui/data.json
    git commit -m "Update fate data"
    git push
